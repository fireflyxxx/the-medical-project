spring:
  application:
    name: medical-imaging-system

  datasource:
    url: jdbc:mysql://localhost:3306/medical_imaging_system?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8mb4
    username: root
    password: 密码
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true